# Phase 4 Implementation Traceability Matrix

Audit date: 2026-09-26. Source: the 7 PDFs in `phase 4/` (Implementation Plan, PM Agent, UI Designer Agent, Prototype Agent, QA/Prototype Validation Agent, Finance Agent, Orchestrator/Router Agent), cross-checked read-only against the repo (`supabase/migrations/`, `src/`, `app/`). No "Phase 4 Master Business Flow" standalone document was found in the repo or in `phase 4/`; all 7 specs refer to it only as an external prior artifact ("the locked Phase 4 flow" / "Phase 4 Master Flow"), not as a file that exists here.

Status legend: **EXISTS** (real, wired, verified by file:line) · **PARTIAL** (some real infrastructure, not the full requirement) · **MISSING** (no trace found) · **DISCONNECTED** (code/table exists but no producer or no consumer) · **UNTESTED** (exists but no test evidence found) · **CONFLICT** (spec assumption contradicts current architecture).

Legend for "Source Doc": Impl = Implementation Plan Checklist Testing Spec; PM = PM Agent spec; UID = UI Designer Agent spec; PROTO = Prototype Agent spec; QAP = QA/Prototype Validation Agent spec; FIN = Finance Agent spec; ORCH = Orchestrator/Router Agent spec.

---

## 1. Cross-cutting infrastructure (must exist before any agent-specific work)

| REQ ID | Source Doc/Section | Requirement | Agent/Module | Existing File | Existing DB | Existing API | Event/Job | Admin UI | Status | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| P4-INFRA-01 | Impl §1, all docs | Phase 3 UI/design baseline (theme, colors, tokens, screens, client-selected design) must exist for Phase 4 to consume | Projects module | `src/modules/projects/design.ts:525` (`record_design_token_set` RPC), `src/modules/projects/queries.ts:1941` (`.from('design_token_sets')`) | `projects.design_token_sets` (`supabase/migrations/20260920000000_the_tokens_phase_four_inherits.sql:54`), `projects.theme_options` (`20260918160000_two_or_three_and_not_one_more.sql:139`), `projects.screens` (`20260821230000_attractive_but_incomplete.sql`, matured `20260918150000`, `20260919220000`, `20260920100000`) | queries.ts / design.ts functions | `project.design_tokens_finalized` event (migration comment, line ~171) | none (`phase-two-panel.tsx` exists; no `phase-three-panel.tsx`/design-baseline panel) | **EXISTS** (baseline is real, not from zero) | Migration filename/comment explicitly say "Phase 4 inherits them rather than recreating them." Application-layer reader/writer confirmed (contradicts an initial pass that found no consumer — `queries.ts:1941` and `design.ts:525` are real callers). No admin UI view yet to browse it. |
| P4-INFRA-02 | Impl §5 WS-01, all docs | `Phase4Workspace`/Task2 bootstrap entity linked to project, Phase 3 baseline, active scope | none found | none | none (`phase4`, `task2`, `phase5_gate` not found in migrations grep) | none | none | none | **MISSING** | Greenfield. No workspace/task-state table for Phase 4 exists anywhere. |
| P4-INFRA-03 | Impl §7, all docs | State machines for Task2/UIVersion/PrototypeBuild/M2 gate | none found | none | none | none | none | none | **MISSING** | No status enum/CHECK constraint for any of these state machines exists in migrations (verified: `ui_version`, `design_qa`, `prototype_build`, `prototype_qa`, `phase5_gate` absent from `supabase/migrations/` by name). |
| P4-INFRA-04 | Impl §8, ORCH §19 | Named events: `UIDesignReady`, `UIDesignQAPassed`, `AdminUIApproved`, `ClientUIApproved`, `PrototypeBuildReady`, `PrototypeQAPassed`, `AdminPrototypeApproved`, `ClientPrototypeApproved`, `Phase4Completed`, `M2PaymentVerified`, etc. | Event/job system | `src/lib/events/catalog.ts` (real, 31 handlers, `HANDLERS`/`SUBSCRIPTIONS`), `src/lib/events/dispatch.ts`, `app/api/jobs/run/route.ts` | outbox/job tables (`20260809120003_outbox_dispatch.sql`, `20260812120009_claim_jobs_by_kind.sql`, etc.) | `dispatchOutbox()` | none of the above Phase 4 event names appear in `catalog.ts` | Agents Automations page shows handoffs, not these events | **MISSING** (infra reusable, events themselves absent) | The catalog's own design rule is "only list what's real" — none of the 12+ Phase 4 event names above are present. The underlying outbox/dispatch mechanism is real and should be extended, not replaced. |
| P4-INFRA-05 | Impl §11 (Orchestrator row), ORCH whole doc | Orchestrator/Router that selects agent+model+enforces permission/tool/version gates | `src/modules/agents/registry.ts:272-294` (typed `orchestrator` AgentDefinition only) | `ai.agents`, `ai.agent_runs` (`20260807120008_ai.sql`), `ai.handoffs` (`20260814120003_...sql`) | none | writes handoff rows via `sales/service.ts` per comment in `app/(internal)/agents/automations/page.tsx:24-27` | `app/(internal)/agents/page.tsx`, `/agents/automations/page.tsx`, `/agents/routing/page.tsx` (read-only) | **MISSING** (real routing code) / **PARTIAL** (typed definition + audit trail exist) | `orchestrator` has `tools: []`, `handoffTargets: []` by design (registry comment: "Naming them now would be inventing a graph the documents do not specify"). No dedicated router/dispatch module found anywhere in `src/` or `app/`. This is the single largest build item — ORCH's entire 20-page contract (capability registry, routing decision, execution envelope, retry/fallback, QA-independence guard, Finance gate routing) has zero implementation. |
| P4-INFRA-06 | Impl §5 WS-11, FIN §5/17 | Finance M2 (20%) milestone triggered by `Phase4Completed`, gated by Admin human verification before Phase 5 | `src/modules/finance/*` (real M1 milestone-invoicing engine) | Extensive real finance schema (`20260807120007_finance.sql` through `20260921150000_...sql`, ~20 migrations) | `finance:generateM1Invoice` handler (`catalog.ts:22`) | `invoice.paid` → `projects:unlockNextMilestone` (real, M1 only) | `app/(internal)/finance/*`, `/invoices/*`, `/invoices/verify/page.tsx` (real M1 verification UI) | **PARTIAL** | M1 (30%) is fully wired end-to-end (invoice → payment → verify → unlock). M2 (20%) does not exist anywhere as a distinct milestone: no `generateM2Invoice` handler, no `Phase4Completed` producer, no M2-specific verification gate. The *pattern* to extend is real and mature — this is a genuine accelerant, not a green-field build. |
| P4-INFRA-07 | Impl §1 | A "Phase 4 Master Business Flow" document exists and this Implementation Plan derives from it | n/a | n/a | n/a | n/a | n/a | **MISSING** | Not found anywhere in the repo (`find`/`grep` across `docs/`, root `.md` files, and `phase 4/` came up empty for that literal title). All 7 specs reference "the locked Phase 4 flow" as an external prior artifact never actually attached to the repo. `AGENCYOS_MASTER_DEVELOPMENT_PLAN.md` (1.2MB, repo root) exists but does not contain the phrase "Master Business Flow" and is a different, already-referenced document. |

---

## 2. Implementation Plan (Impl) — cross-agent contracts

| REQ ID | Source | Requirement | Existing evidence | Status | Notes |
|---|---|---|---|---|---|
| P4-IMPL-ENTITIES | Impl §6 p.5 | `Phase4Workspace`, `UIScreen`, `UIVersion`, `UICoverageRecord`, `DesignQAReport`, `UIRevision`, `PrototypeBuild`, `PrototypeQAReport`, `PrototypeRevision`, `PMCommunication`, `Phase4Handoff`, `AuditEvent` | None of these table names found in `supabase/migrations/` | **MISSING** | `AuditEvent`-equivalent concept may already exist generically elsewhere in the schema (not confirmed in this pass — recommend a follow-up grep for a generic audit-log table before building a new one, per the spec's own "reuse existing canonical entities" rule, Impl §6). |
| P4-IMPL-SM-TASK2 | Impl §7.1 | Task2/Phase4 state machine: NOT_STARTED → ... → PHASE5_READY | None found | **MISSING** | |
| P4-IMPL-SM-UIVER | Impl §7.2 | UIVersion state machine: DRAFT → QA_REVIEW → ... → LOCKED | None found | **MISSING** | |
| P4-IMPL-SM-PROTO | Impl §7.3 | PrototypeBuild state machine: PLANNED → ... → LOCKED | None found | **MISSING** | |
| P4-IMPL-SM-M2GATE | Impl §7.4 | M2 Gate: M2_NOT_DUE → ... → PENDING_VERIFICATION → VERIFIED | Compare to real M1 flow: `PAYMENT_PENDING`/verification states exist in finance migrations for M1; no M2-specific instance | **PARTIAL** (pattern exists for M1 only) | |
| P4-IMPL-ADMINUI | Impl §10 p.6-7 | 14 required Admin Panel surfaces (Phase4 Overview, Phase3 Baseline, UI Coverage, Full UI, Design QA, Admin/Client UI Review, Prototype Builds/QA/Review, PM Communication, Finance M2, Audit) | **Phase4 Overview implemented**: `app/(internal)/projects/[projectId]/phase-four-panel.tsx` + `readPhaseFourOverview` (`src/modules/projects/queries.ts`), wired into the real project detail page (`page.tsx`), directly answering Master's own checklist ("WHAT IS CURRENT? WHAT VERSION? IS M2 VERIFIED? CAN PHASE 5 START?") from one read. Admin/Client UI Review and Prototype Review both reuse EXISTING generic surfaces built in earlier steps (`/approvals/[requestId]`, the `/prototype` page's artifact_url link) rather than new screens. **Design QA, UI Coverage, PM Communication and Finance M2 dashboards remain missing as dedicated screens** — their underlying data (`qa_findings`, `ai.handoffs`, invoice/payment tables) is all real and queryable, just not yet surfaced as their own Admin Panel pages. | **PARTIAL** |
| P4-IMPL-TESTCAT | Impl §16 | 60 test cases P4-T001–T060 across 13 layers | No Phase 4 test files found | **MISSING** | |
| P4-IMPL-DOD | Impl §19 | 21-item Definition of Done | N/A (nothing built yet) | **MISSING** | |

---

## 3. PM Agent

| REQ ID | Source | Requirement | Existing evidence | Status | Notes |
|---|---|---|---|---|---|
| P4-PM-AGENT-DEF | PM §1 | PM Agent coordinates Task 2 client communication | `PROJECT_MANAGER` AgentDefinition, `src/modules/agents/registry.ts:402-427`; tools `['projects.readScope','memory.recall','memory.remember','projects.submitChangeRequest','approvals.requestApproval','crm.sendClientMessage']`; `handoffTargets: ['ui_designer','developer','quality_assurance']` | **PARTIAL / DISCONNECTED** | Agent is typed and tool-bound in code, but per registry comment (lines 341-344) it is seeded **disabled** in the DB — no evidence of a live run. `crm.sendClientMessage` tool exists in the type system but activation is separate from capability. |
| P4-PM-MSG-CONTRACT | PM §5-6, 13 | 8 message templates (PM4-M01–M08); `PMCommunication`, `ClientReviewShare`, `ClientFeedback`, `RevisionRequest`, `ClarificationRequest`, `PMEscalation`, `TaskAnnouncement` data contracts | **PM4-M01–M07 implemented and reachable; only PM4-M08 remains.** Seven handlers in `src/modules/crm/handlers.ts` (`announcePhaseFourStarted`, `announceUiVersionAdminReviewed`, `announceUiVersionChangeRequested`, `announceUiVersionLocked`, `announcePrototypeSubmitted`, `announcePrototypeChangeRequested`, `announceTask2Complete`), all sharing one `announceToInternalChannel` helper (extracted rather than copied a seventh time) and all modeled on the pre-existing `handlePhaseThreeCompleted`. Closed real gaps of the identical shape along the way: `project.ui_version_admin_reviewed`, `project.ui_version_client_decided`, `project.deliverable_submitted` all had zero subscribers before this. M05–M07 became reachable once `20260923160000_m2_and_the_gate_it_actually_needs.sql` added the generic `project.deliverable_submitted`/`project.deliverable_decided` events (needed for Finance M2 anyway) — each PM handler filters to `kind = 'prototype'` itself, the same division `handleDeliverableDecided` keeps, so the shared deliverable SQL stays kind-agnostic. **Deliberate divergence from PM §5's letter, named rather than silently reconciled:** PM §5 describes all of these as direct client sends; this codebase's real precedent for the identical Phase 3 milestone announces to the INTERNAL group instead (staff relay to the client over WhatsApp, per ADM-08d) — no automated client-send path exists for Task 2 milestones beyond the two already-reviewed exceptions (`dispatchApprovedQuotation`, `reply.due`). **PM4-M08 is now also implemented — all 8 messages complete.** No new gate or event was needed: `invoice.paid` already fires only once `finance.verify_payment_submission` (a real Admin session, pre-existing) verifies a payment in full, for any milestone. `announceM2PaymentVerified` is a second, independent subscriber to that same event, filtering to `position = 2` by re-reading the milestone row rather than trusting the payload. Its own event schema is a small crm-local copy (`invoicePaidForM2EventSchema`) rather than a cross-module import of `projects/schema.ts`'s version, per ARCHITECTURE.md §3.2 (cross-module access goes through service.ts only) — the same discipline every other event schema in `crm/schema.ts` already keeps. | **DONE** |
| P4-PM-STATE | PM §10 | 14 PM waiting-state values (WAITING_DESIGN, WAITING_QA, ..., WAITING_M2, BLOCKED) | None found | **MISSING** | |
| P4-PM-REVISION-LIMIT | PM §9 | Revision counting, ~2-3 client rounds then escalation | `handleRevisionLimitEscalated` exists in `src/modules/crm/handlers.ts` (name strongly suggests an existing revision-limit concept, likely for sales/quotation flows, not Phase 4 UI/prototype) | **PARTIAL** (pattern exists elsewhere, not for Phase 4) | Needs verification of what `handleRevisionLimitEscalated` currently limits — recommend confirming it is not already coincidentally reusable before building a parallel counter. |
| P4-PM-BOUNDARIES | PM §11 | PM must not: design, QA, approve, verify payment, bypass revision limits, leak provider details | N/A — no PM Phase-4 code exists to violate or honor these yet | **MISSING** (nothing to test) | |
| P4-PM-TESTS | PM §21-22 | 28 test cases, 19-step E2E scenario | None found | **MISSING** | |

---

## 4. UI Designer Agent

| REQ ID | Source | Requirement | Existing evidence | Status | Notes |
|---|---|---|---|---|---|
| P4-UID-AGENT-DEF | UID header, §1 | Conditionally-activated UI Designer Agent; consumes locked Phase 3 baseline | `UI_DESIGNER` AgentDefinition, `registry.ts:431-449` | **PARTIAL/DISCONNECTED** | Typed + tool-bound but seeded disabled (same as PM); no run evidence. |
| P4-UID-ENTITIES | UID §19 p.9 | `UIDesignJob`, `UIVersion`, `UIScreenVersion`, `UIDesignRevision`, `UIDesignBlocker`, `DesignQADefectLink`, `FigmaArtifactRef` | None found in migrations. Related-but-different: `projects.design_token_sets`, `projects.theme_options`, `projects.screens` (Phase 3 tables) | **MISSING** (Phase 4 entities), **EXISTS** (Phase 3 tables they'd extend) | Phase 4 spec's "reuse existing canonical entities" instruction (Impl §6 note) applies directly here — `projects.screens` should likely be versioned/extended rather than replaced by a new `UIScreenVersion` table from scratch. |
| P4-UID-EVENTS | UID §20 | `Phase4Started`, `UICoverageGapConfirmed`, `DesignQAChangesRequired`, `AdminUIEditRequested`, `ClientUIChangeApproved`, `ScopeChangeApprovedForUI`, `ClientUIApproved`, `PrototypeDesignIssueConfirmed` | `project.phase_four_started` (real, 20260923100000) and `project.ui_version_drafted` (real, 20260923110000) now exist and fire; the QA/Admin/client events in this row remain **MISSING**. | **PARTIAL** | Note: UID's own event names (`DesignQAChangesRequired`) differ slightly from ORCH's (`UIDesignQAChangesRequired`) — a naming inconsistency across the specs themselves, flagged for whoever defines the canonical catalog entry. |
| P4-UID-RESP | UID §4, §6 | Design every required screen/state/variant; reuse Phase 3 tokens; store exact Figma/version refs | **Implemented (draft stage only).** `projects.ui_versions` (migration `20260923110000_the_ui_version_designs_the_locked_screens.sql`) + `projects.record_ui_version_draft` door + the `ui_designer:draftUIVersion` workflow (`app/api/jobs/run/workflows.ts`, job kind `ui.version_draft`, triggered by `project.phase_four_started`). Reads screens and tokens through the FROZEN `phase_three_handoffs.payload` (UID §19's reuse rule), rejects any screen key the model invents, and is idempotent per workspace. Live-verified on a scratch Postgres end to end (drafted / already_drafted replay / wrong_state / the row constraint). **Deliberately not pixel/Figma output** — see the schema's own docblock for why that stays `MANUAL_FIGMA_REQUIRED` per the master prompt's anti-fake-capability rule. Design QA, Admin review, client review and the revision loop are NOT built yet (`ui_versions.status` only ever reaches `draft`). | **PARTIAL** |
| P4-UID-BOUNDARY | UID §17 | Designer must not self-QA/self-approve/bypass scope/leak provider info | N/A | **MISSING** (nothing to test) | |
| P4-UID-ADMINUI | UID §18 | 9 Admin surfaces (UI Design Overview, Phase 3 Baseline, Coverage Matrix, UI Versions, Design QA, Admin/Client Review, Figma Source, Audit) | None Phase-4-specific; `app/(internal)/projects/[id]/phase-two-panel.tsx` is the nearest sibling pattern | **MISSING** | |
| P4-UID-DESIGN-QA | UID §19; QAP §7; ADM-82 | Design QA independently verifies a UI version's coverage before Admin/client review; the producer may not verify its own work | **Implemented (coverage checks only).** `projects.ui_versions.qa_findings`/`qa_reviewed_at` (migration `20260923120000_qa_cannot_pass_its_own_design.sql`) + `projects.record_ui_version_qa_verdict` door + `src/modules/qa/handlers.ts` (`handleReviewUIVersion`), triggered by `project.ui_version_drafted`. Screen coverage and declared-state coverage against the locked baseline are checked deterministically; the verdict itself is decided by `verdictFor` (`src/modules/agents/verification.ts`) — the SAME ADM-82 producer≠verifier contract used elsewhere, not a bespoke rule, reusing `ui_designer.verification.verifiedBy: 'quality_assurance'` which has been on the registry since it was written. Live-verified on scratch Postgres: a genuine coverage gap produces `qa_changes_required` with named findings, full coverage produces `qa_pass`, and a replay cannot overwrite an existing verdict. **Consistency, token usage and usability remain unchecked** — named explicitly in the migration and handler as a scope boundary, not a silent gap. | **PARTIAL** |
| P4-UID-TESTS | UID §26-28 | 30 test cases, conditional E2E scenario | None found | **MISSING** | |

---

| P4-UID-ADMIN-REVIEW | Impl §7.2; Master locked objective ("QA PASS → ADMIN REVIEW") | Admin CONFIRM/EDIT gate on a qa_pass UI version, before it may reach a client | **Implemented, reusing the existing approvals engine — not a new mechanism.** `ui_version` added to `approvals.approval_requests`/`approval_policies`'s `subject_type` CHECK (migration `20260923130000_admin_review_reuses_the_engine.sql`), the exact pattern `20260813120003_handover.sql` already used to extend the same engine. `projects.request_ui_version_admin_review` raises an internal-audience request via `approvals.request_approval` when `ui_designer:reviewUIVersion`'s `qa_pass` fires; `projects.sync_ui_version_decision` pulls a settled decision back (`approved→admin_approved`, `rejected`/`changes_requested`→`admin_edit`, collapsing to Master's CONFIRM/EDIT binary), wired into the EXISTING generic `approvals` Admin Panel UI (`app/(internal)/approvals/[requestId]/page.tsx`, `carryDecisionToSubject`) with zero new Admin UI built. `APPROVAL_SUBJECT_TYPES` (`src/modules/approvals/schema.ts`) also updated so an owner can actually configure a `ui_version` policy through the existing Settings form — without it the feature would be permanently `no_policy`-blocked. Live-verified end-to-end on scratch Postgres: honest `no_policy` refusal before an owner configures one, then a real `request_approval`→`decide_approval`(as an authenticated `ops_admin`)→`sync_ui_version_decision` round trip producing both `admin_edit` and `admin_approved`. **No policy is seeded for any organization** — same as `handover`/`prototype`/`agent_action` before it, this is a genuine manual Admin-Panel setup step, not a bug. | **PARTIAL** |
| P4-UID-CLIENT-REVIEW | Impl §7.2; Master locked objective ("PM SHARE → CLIENT... CLIENT APPROVES EXACT UI → LOCK") | Client-facing review/approval of an admin_approved UI version, then UI lock | **Implemented as a bespoke, proven-shape log — not the untested generic approvals `audience='client'` path.** Migration `20260923140000_the_client_confirms_the_locked_ui.sql`: `projects.ui_version_client_decisions` (append-only, mirrors `client_design_decisions`'s verbatim-words/evidence_ref/recorded_by shape, refuses UPDATE/DELETE by trigger) + three doors — `share_ui_version_with_client` (admin_approved→client_review), `record_ui_version_client_decision` (→client_change or client_approved, two decisions only: Master names exactly two client actions here, not Phase 3's six), `lock_ui_version` (client_approved→locked). A `freeze_locked_ui_version` trigger makes the lock structural: proven live that even a service-role UPDATE against a locked row is refused. Live-verified end-to-end on scratch Postgres: full two-round flow (share → change_requested → final_confirmed → lock), an idempotent replayed lock, wrong-state refusals at every premature step, and the append-only trigger genuinely refusing an edit to a settled decision. **No Admin/PM-facing UI form exists yet for these three doors** — this is real, callable, tested backend capability without a screen in front of it, an explicit and named boundary rather than a silent gap. **The revision loop is now built** (migration `20260924100000_the_revision_loop_client_change_asked_for.sql`, this session): `ui_versions`'s unique constraint moved from `(phase_four_id)` to `(phase_four_id, version)` so a round is a second row rather than a mutated first one; `projects.revise_ui_version` drafts round N+1 from a `client_change` version, reusing `project.ui_version_drafted` so Design QA needs no new subscription; `phase_four.ui_revision_count`/`ui_revision_limit` are now read and enforced in the door itself, not just displayed — a round past the limit stops the workspace at `revision_limit_escalation` with a `blocked_reason` and drafts nothing, Master's "after limit: HUMAN ESCALATION." A new `ui_designer:reviseUIVersion` AI workflow (job kind `ui.version_revise`) subscribes to `project.ui_version_client_decided`, filters to `change_requested`, checks the limit before any model call, and calls the door. Live-verified end-to-end on scratch Postgres: draft→client_change→revise produces a real second row without disturbing the first; a replay against an already-drafted round answers `already_revised`; three full rounds reach the configured limit of 3; a fourth is refused with `revision_limit_reached`, and — confirmed directly — no fifth row is written. | **PARTIAL** |
> **Step 4 status (this session):** implemented and live-verified. See P4-PROTO-AGENT-DEF, P4-PROTO-ENTITIES, P4-QAP-AGENT-DEF rows below for what changed. Headline finding: `projects.deliverables` (kind='prototype') already had a real, working draft→in_review→approved lifecycle with a genuine client-audience approval request and a live Admin Panel screen — Phase 4's Prototype stage reuses it entirely rather than building a second one. What was actually missing was (a) safe structured content a client could click through, and (b) the one gate `deliverables` itself lacks: independent QA before a human decides to submit.

## 5. Prototype Agent

| REQ ID | Source | Requirement | Existing evidence | Status | Notes |
|---|---|---|---|---|---|
| P4-PROTO-AGENT-DEF | PROTO header | Builds versioned interactive prototype from exact locked UI version | **Implemented and live-verified.** `ui_prototype` is actually ENABLED (confirmed: `20260822150000`-era activation, not disabled as originally audited) and now has its first real workflow: `PROTOTYPE_BUILD` (`app/api/jobs/run/workflows.ts`, job kind `prototype.build`), triggered by `project.ui_version_locked`. Generates a structured, safe interaction spec (never raw HTML — see `prototypeBuildSchema`'s docblock for the stored-XSS reasoning), rejects invented screens and broken navigation targets before persisting (QAP's own "broken route" negative test, checked in-workflow). | **PARTIAL** |
| P4-PROTO-ENTITIES | PROTO §12 p.7 | `PrototypeBuild`, `PrototypeRouteCoverage`, `PrototypeTestData`, `PrototypeRevision`, `PrototypeQAReport`, `ArtifactRecord`, `PrototypeBlocker` | **Reuses `projects.deliverables` (kind='prototype') for the whole lifecycle** — real draft→in_review→approved/changes_requested states, version sequence, artifact_url, and a REAL client-audience `approvals.request_approval` call (`submit_deliverable`, pre-existing, confirmed genuinely exercised) — rather than inventing PrototypeBuild/PrototypeRevision as new entities. `projects.prototype_artifacts` (migration `20260923150000_the_prototype_reuses_the_deliverable.sql`) is the one new table: the structured content + `qa_findings`/`qa_reviewed_at` deliverables itself has no room for. `record_prototype_build` calls the EXISTING `add_deliverable` door — no parallel creation mechanism. | **PARTIAL** |
| P4-PROTO-SM | PROTO §11 p.7 | PLANNED → INPUT_VALIDATION → BUILDING → BUILD_READY → QA_REVIEW → ... → LOCKED | None found | **MISSING** | |
| P4-PROTO-OUTPUT-TYPES | PROTO §5 | Web preview / Android APK / iOS preview / desktop, project-specific (not hardcoded) | No build pipeline of any kind found in repo for prototype artifacts | **MISSING** | Also implies external dependency risk: signing/distribution credentials (Apple/Google) are explicitly called out in the spec (§22) as manual/external blockers — none of this infra exists. |
| P4-PROTO-TRACE | PROTO §8 | Every build stores `source_ui_version_id`; rejects non-locked UI | Depends on P4-UID-ENTITIES (UIVersion) existing first — it does not | **MISSING** (blocked on UI Designer work) | |
| P4-PROTO-BOUNDARY | PROTO §4 | Must NOT own: production backend, real payment settlement, Phase 5 business logic, self-QA/approval | N/A | **MISSING** (nothing to test) | |
| P4-PROTO-ADMINUI | PROTO §13 | Prototype Overview, Build History, Coverage, Artifacts, Limitations, QA, Admin/Client Review, Revision Timeline, Blockers, Audit | None found | **MISSING** | |
| P4-PROTO-TESTS | PROTO §20-21 | 32 test cases, 19-step E2E | None found | **MISSING** | |

---

## 6. QA / Prototype Validation Agent

| REQ ID | Source | Requirement | Existing evidence | Status | Notes |
|---|---|---|---|---|---|
| P4-QAP-AGENT-DEF | QAP header | Independently validates exact prototype build vs. locked UI; cannot be self-certified by Prototype Agent | **Implemented and live-verified.** `handleReviewPrototypeBuild` (`src/modules/qa/handlers.ts`), triggered by `project.prototype_build_ready`. Computes two objective coverage checks (missing screens vs. the locked UI version, broken navigation targets within the build) and decides the verdict through `verdictFor` — the SAME ADM-82 producer≠verifier contract Design QA reuses, with `ui_prototype.verification.verifiedBy: 'quality_assurance'` already on the registry. Deliberately does not touch `projects.deliverables.status` — the human "Submit for review" click on the existing prototype Admin Panel page stays the real gate, informed by `qa_findings` rather than bypassed. Live-verified on scratch Postgres: a genuine broken-route finding produces `qa_changes_required`, idempotent replay refuses to overwrite a verdict. | **PARTIAL** |
| P4-QAP-ENTITIES | QAP §19 p.9 | `PrototypeQARun`, `PrototypeQACheck`, `PrototypeDefect`, `PrototypeRetest`, `PrototypeQAEvidence`, `PrototypeQABlocker` | None found. Existing generic QA dashboard exists: `app/(internal)/qa/page.tsx` (org-wide QA dashboard per commits `56cf44a`/`e302682`/`7a79651`) but scope/purpose not confirmed to match Phase 4 prototype validation | **MISSING** (Phase-4-specific), **worth checking** whether `/qa/page.tsx`'s underlying schema is repurposable | |
| P4-QAP-OUTCOMES | QAP §14 | 4 decision outputs: QA_PASS, CHANGES_REQUIRED, BLOCKED_EXTERNAL, INVALID_INTAKE | `verification.ts` has a generic verdict enum (`verdictFor`/`decideVerdict`, lines 117-238) — not confirmed to match these exact 4 states | **PARTIAL** (generic verdict machinery exists; Phase-4-specific states do not) | |
| P4-QAP-DEFECT-SEVERITY | QAP §7 | P0/P1/P2/P3/Enhancement severity classification | Not found; may overlap with existing `qa-panel.tsx` defect tracking on project detail pages — unconfirmed | **UNTESTED/MISSING** | Recommend checking `app/(internal)/projects/[id]/qa-panel.tsx` schema before building a parallel severity model. |
| P4-QAP-ADMINUI | QAP §18 | Prototype QA Overview, Validation Matrix, Defects, Retests, Regression, Build History, Limitations, Readiness, Audit | `app/(internal)/qa/page.tsx` exists generically | **PARTIAL** | |
| P4-QAP-TESTS | QAP §26-27 | 32 test cases, 18-step E2E | None found | **MISSING** | |

---

## 7. Finance Agent (M2 = 20% milestone + hard human-verification gate)

| REQ ID | Source | Requirement | Existing evidence | Status | Notes |
|---|---|---|---|---|---|
| P4-FIN-AGENT-DEF | FIN header | Finance Agent creates M2 workflow after `Phase4Completed`; never self-verifies payment | `FINANCE` AgentDefinition, `registry.ts:502-522`; `moneyAuthority: 'proposes_for_approval'`; tools `['memory.recall','finance.generateInvoice','approvals.requestApproval']` | **PARTIAL/DISCONNECTED** | Typed, seeded disabled, no run evidence. `moneyAuthority: 'proposes_for_approval'` is a directly on-point existing guardrail matching the spec's "Finance manages the workflow, Admin verifies" principle — good precedent to extend for M2. |
| P4-FIN-ENTITIES | FIN §20 p.8 | `FinancialMilestone`, `Invoice`, `PaymentInstructionSnapshot`, `PaymentSubmission`, `PaymentMatchResult`, `PaymentVerification`, `Receipt`, `FinanceException`, `PaymentReminder` | Real analogs exist for M1: extensive invoice/payment schema across `20260807120007_finance.sql` ... `20260921150000_...sql` (~20 migrations); `20260811120005_invoice_issue_serialized.sql`, `20260815300000_a_manual_payment_can_actually_be_verified.sql`, `20260821250000_a_payment_is_claimed_then_verified.sql`, `20260822260000_reconciliation_says_what_it_found.sql` | **PARTIAL** (mature engine exists, needs M2-specific extension not a rebuild) | This is the strongest existing foundation in the entire Phase 4 scope — the M1 invoice/payment/verification/reconciliation lifecycle is real, tested, and directly analogous. |
| P4-FIN-MILESTONE-STRUCTURE | FIN §2 p.2 | Global 30/20/30/20 (M1/M2/M3/M4) structure; this doc implements only M2 | **Implemented and live-verified.** `finance:generateM2Invoice` (`generateM2Invoice`, `src/modules/finance/service.ts`) is a deliberate SIBLING of `generateFirstMilestoneInvoice` (M1), not a parameterized generalization of it — reuses every already-factored-out helper (`milestoneInvoiceability`, `billingReadiness`, `taxRateBpForMode`, `milestoneInvoiceLines`, `invoiceTotals`, `nextInvoiceNumber`, `create_milestone_invoice`) identically, duplicating only orchestration order, to avoid touching tested, revenue-critical M1 code. Confirmed via research that `projects.milestones`/`finance.create_milestone_invoice`/`nextUnlockedMilestone` were ALREADY fully milestone-generic — M1 was the only hardcoded caller, not the schema. | **PARTIAL** (M1, M2 done; M3/M4 not yet triggered — same pattern extends when Phase 5/6 exist) |
| P4-FIN-GATE-MECHANICS | FIN §12-17 (the core ask) | Hard gate: only Admin-verified `M2PaymentVerified` opens Phase 5; `Phase4Completed`/`InvoiceIssued`/`PaymentSubmitted`/`ProofUploaded`/Finance-recommended-match do NOT open it; server/workflow-enforced, not just UI-hidden | **Confirmed already real and generic — required zero new gate mechanism.** `finance.verify_payment_submission` (pre-existing) is reached exclusively through a real signed-in Admin session (`app/(internal)/invoices/verify/page.tsx`, `security invoker`, no service_role/job path) and is the ONLY thing that can move `finance.invoices.status` to `'paid'` — for any milestone position, not just M1. `generateM2Invoice` never touches payment status at all (live-verified: no `.update({status:'paid'})`, no reference to `verify_payment_submission`). New: `projects.complete_phase_four` (Task 2 closes on the final prototype's client approval, via a NEW generic `project.deliverable_decided` event added to the shared `sync_deliverable_decision`/`submit_deliverable` functions — additive, `pg_get_functiondef`-sourced, filtered to `kind='prototype'` in TypeScript so the shared SQL stays kind-agnostic) → emits `project.phase_four_completed` → triggers `generateM2Invoice`. `projects.phase_five_gate_status(project_id)` is a new read-only function answering Master's own "CAN PHASE 5 START?" directly (`not_ready`/`invoice_issued`/`verified`), live-verified through the `not_ready`→`invoice_issued` transition on scratch Postgres (the final `invoice_issued`→`verified` arm confirmed by regex + the identical CASE structure). | **PARTIAL — the hard gate itself is DONE; Phase 5 has no code yet to actually consume the gate, since Phase 5 is out of scope** |
| P4-FIN-BOUNDARY | FIN §18 | Finance must NOT: mark verified from client message/screenshot, self-approve, guess tax, duplicate M2 invoice on replay | `moneyAuthority: 'proposes_for_approval'` (registry.ts) enforces the self-approval boundary conceptually; idempotency for M1 invoice generation presumably exists (not directly verified this pass) | **PARTIAL** | Recommend confirming `finance:generateM1Invoice`'s replay-idempotency behavior as a template before assuming M2 replay-safety "for free." |
| P4-FIN-ADMINUI | FIN §19 | M2 Overview, Invoice, Payment Submission, Verification Queue, Receipt, Reminders, Exceptions, Project Finance View, Audit | `app/(internal)/finance/*`, `/invoices/*`, `/invoices/verify/page.tsx`, `/finance/payments/page.tsx` all real for M1 | **PARTIAL** (real for M1, not extended to M2) | |
| P4-FIN-TESTS | FIN §28-29 | 36 test cases, 12-step E2E, negative/exception scenarios | None found | **MISSING** | |

---

## 8. Orchestrator/Router Agent

| REQ ID | Source | Requirement | Existing evidence | Status | Notes |
|---|---|---|---|---|---|
| P4-ORCH-AGENT-DEF | ORCH header | Routes Phase 4 tasks to correct specialist+model+tool path; does not become the specialist or approval authority | `src/modules/orchestrator/route.ts` (`decideAgentForTask`) + `src/modules/orchestrator/handlers.ts` (`handleRouteTask2Design`), wired via `project.phase_four_started` → `orchestrator:routeTask2Design` in `catalog.ts` and drained in `app/api/jobs/run/route.ts` | **PARTIAL — MVP implemented and live-verified** | Corrects the original audit finding: `ai.handoffs` + `ai.agent_handoff_targets` already existed, fully real (RLS, the `ai.enforce_handoff_target` trigger, `check-record` §16 drift-check) with exactly one hardcoded producer (`sales`→`project_manager` at WON) and a real generic Admin reader (`listHandoffs`, Automations page) that had never had a second writer. Task 2's first hop (`project_manager`→`ui_designer`, chosen by capability from the registry, not hardcoded) now routes through this exact infrastructure — proven end-to-end on a scratch Postgres, including the DB trigger genuinely refusing an undeclared target (`project_manager`→`finance`). Model/provider selection, cost/quality/latency ranking and the fuller `RoutingDecision`/`ExecutionEnvelope` entities remain out of scope per `docs/phase-4-gap-analysis.md` step 2 and are still MISSING. `orchestrator` the AgentDefinition remains unused as a graph node (deliberately — see route.ts docblock); the routing ENGINE is the new real piece, not activation of that identity. |
| P4-ORCH-ENTITIES | ORCH §29 p.11 | `AgentCapability`, `RoutingDecision`, `ExecutionEnvelope`, `ExecutionRecord`, `RouteFailure`, `FallbackRecord`, `OrchestratorEscalation`, `UsageCostRecord` | None found. Nearest generic analog: `ai.agents`, `ai.agent_runs` (`20260807120008_ai.sql`) track run identity but not a capability/routing-decision/envelope model | **MISSING** | |
| P4-ORCH-EVENT-MAP | ORCH §19 p.8 | Routes every Phase 4 event (`Phase3Completed` → ... → `M2PaymentVerified`) to the correct specialist | None of these events exist in `catalog.ts`; no consumer code | **MISSING** | Depends entirely on P4-INFRA-04 (events) existing first. |
| P4-ORCH-GUARDS | ORCH §10, §21, §22 | Routing guards: block Designer without activation reason, QA-independence (agent cannot validate own work), Finance-gate routing (no AI route can claim final payment verification) | `verification.ts` already enforces producer≠verifier generically (`mayVerify` true only for QA) — directly reusable scaffolding for the QA-independence guard specifically | **PARTIAL** (QA-independence precedent exists genuinely; Designer-activation and Finance-gate guards do not) | This is the one Orchestrator sub-requirement with a real, non-trivial existing precedent — worth highlighting as an accelerant. |
| P4-ORCH-ADMINUI | ORCH §28 p.11 | Routing Dashboard, Task Trace, Agent Registry, Model/Provider Routing, Tool Permissions, Phase 4 Trace, Failure Queue, Retry/Fallback, Budget/Usage, Escalations, Audit | `app/(internal)/agents/page.tsx`, `/agents/[agentKey]/page.tsx`, `/agents/routing/page.tsx` + `routing-form.tsx` (read-only registry views exist) | **PARTIAL** | Existing Agent Registry admin views are a real, reusable starting point for the "Agent Registry" and partially "Model/Provider Routing" surfaces; the other 9 surfaces (Routing Dashboard, Task Trace, Failure Queue, etc.) do not exist because there is no routing engine yet to display data from. |
| P4-ORCH-TESTS | ORCH §32-33 | 40 test cases, 20-step E2E | None found | **MISSING** | |

---

## 9. Summary counts

- Total requirement rows in this matrix: **48** (grouped from ~150+ granular test cases and ~40 entity/event/state-machine/responsibility items across the 7 specs; see each section's "Notes" for the underlying detail this was rolled up from).
- **EXISTS**: 1 (P4-INFRA-01, Phase 3 design-token/screen baseline)
- **PARTIAL**: 20 (mostly: agent definitions typed-but-disabled; M1 finance/invoice engine as a pattern for M2; QA verification contract; admin registry views; approvals-permalink UI pattern)
- **MISSING**: 26 (all Phase-4-specific entities, events, state machines, Admin UI surfaces, and all test suites)
- **DISCONNECTED**: overlaps with PARTIAL rows above — every one of the 6 Phase 4 agents (`pm`/`project_manager`, `ui_designer`, `ui_prototype`, `quality_assurance`, `finance`, `orchestrator`) is defined in `src/modules/agents/registry.ts` but seeded **disabled**, with only `requirement_collector` ever having actually run in production.
- **CONFLICT**: 1 — see gap analysis: the spec's assumption that "Prototype Validation QA" is a role distinct from the general `quality_assurance` agent may not map cleanly onto the current single-QA-agent registry design without an explicit sub-role/capability split.
