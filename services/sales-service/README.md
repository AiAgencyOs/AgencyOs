# sales-service

Purpose: Handle lead ingestion, sales pipeline, quotations, and billing integration.

APIs: Expose REST/GraphQL endpoints for querying leads, proposals, and invoices.

Data stores: PostgreSQL (Supabase) for authoritative data.
